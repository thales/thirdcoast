<div class=rightCol>
<h1>New staff_pick</h1>

<% form_for([:admin, @staff_pick]) do |form| %>
  <%= render :partial => 'form', :locals => {:form => form} %>
    <p><%= form.submit 'Create' %></p>
<% end %>
 
<%= link_to 'Back', admin_staff_picks_path %>
</div> 