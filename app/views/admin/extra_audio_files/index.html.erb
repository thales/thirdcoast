<h1>Listing extra_audio_files</h1>

<table>
  <tr>
    <th>Description</th>
  </tr>

<% @extra_audio_files.each do |extra_audio_file| %>
  <tr>
    <td><%=h extra_audiao_file.description %></td>
    <td><%= link_to 'Show', extra_audio_file %></td>
    <td><%= link_to 'Edit', edit_extra_audio_file_path(extra_audio_file) %></td>
    <td><%= link_to 'Destroy', extra_audio_file, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New extra_audio_file', new_extra_audio_file_path %>