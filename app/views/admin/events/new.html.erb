<div class="rightCol">
  <h1>New Event</h1>

  <% form_for([:admin, @event], :html => { :multipart => true} ) do |form| %>
    <%= render :partial => 'form', :locals => {:form => form} %>
    <p><%= form.submit 'Create' %></p>
  <% end %>

  <p><%= link_to 'Back', admin_events_path %></p>
</div>